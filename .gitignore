__pycache__/
.DS_Store
.idea
.ruff_cache
.venv
.vscode
*.py[cod]
*.egg-info
venv
.claude
.DS_Store

# kbc datafolder
/data

# vcr datadirtesting
secrets.json
# Derived from secrets.json by tests/setup/make_badpassword_secrets.py: real tenant + a
# deliberately wrong password, used for the bad-credentials recording pass. Carries the real
# tenant, so it is the same sensitivity class as secrets.json.
secrets_badpassword.json
tests/functional/*/source/data/config.secrets.json
tests/functional/*/source/data/out/
tests/functional/*/source/data/in/tables/

# component-factory local build artifacts (never committed)
docs/superpowers/*-lifecycle.md
docs/superpowers/research/
.claude/loop.md